import React from 'react';
import {createRoot} from 'react-dom/client';

const ToDoList = () => {
  return 
}

const AppHeader = () => {
  return
}

const SearchPanel = () => {
  return
}

const el = <h1>Hello</h1>;
console.log(el);

const root = createRoot(document.querySelector('section'));
root.render(el);
